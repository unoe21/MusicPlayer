<UserControl x:Class="MusicPlayer.MVVM.View.PlaylistsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicPlayer.MVVM.View"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">

    <Grid Background="#091d38">
        <Grid Margin="40 58 40 40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="10 0 10 20">
                <TextBlock Text="Playlists" Style="{StaticResource titleText}" VerticalAlignment="Center"/>

                <Button Command="{Binding StartCreatePlaylistCommand}" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Background="#fc3c44" Foreground="White" BorderThickness="0" 
                        Padding="20 10" Cursor="Hand" FontSize="14" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="20"/>
                        </Style>
                    </Button.Resources>
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Plus" Width="14" Height="14" VerticalAlignment="Center" Margin="0 0 8 0"/>
                        <TextBlock Text="Új Playlist" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>

            <ListBox Grid.Row="1" 
                     ItemsSource="{Binding Playlists}" 
                     SelectedItem="{Binding SelectedPlaylist, Mode=TwoWay}"
                     SelectionMode="Single"
                     Background="Transparent" 
                     BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Margin" Value="0 0 0 10"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="15">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#66000000"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Width="55" Height="55" CornerRadius="12" Background="#fc3c44">
                                <Icon:PackIconMaterial Kind="PlaylistMusic" Width="28" Height="28" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20 0 0 0">
                                <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding Tracks.Count, StringFormat='{}{0} dal'}" FontSize="14" Foreground="LightGray" Margin="0 4 0 0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Border x:Name="Overlay" Panel.ZIndex="100" Background="#CC000000">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsCreatingPlaylist}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Border Background="#091d38" CornerRadius="25" Width="500" Height="550" 
                    BorderBrush="#fc3c44" BorderThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="50" Color="Black" Opacity="0.9"/>
                </Border.Effect>

                <Grid Margin="30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Új Lejátszási Lista" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0 0 0 20"/>

                    <Grid Grid.Row="1" Margin="0 0 0 20">
                        <TextBox x:Name="NameInput" Text="{Binding NewPlaylistName, UpdateSourceTrigger=PropertyChanged}" 
                                 Background="Transparent" Foreground="White" FontSize="16" 
                                 BorderBrush="#fc3c44" BorderThickness="0 0 0 2" Padding="5" 
                                 CaretBrush="White"/>

                        <TextBlock Text="Adj meg egy nevet..." Foreground="Gray" FontSize="16" Margin="6" IsHitTestVisible="False" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=NameInput}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Text, ElementName=NameInput}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <Border Grid.Row="2" Background="#1a2c47" CornerRadius="10" Margin="0 0 0 25">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding AvailableTracksForPlaylist}" Margin="10">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected}" Margin="5" Cursor="Hand">
                                            <CheckBox.Style>
                                                <Style TargetType="CheckBox">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Style>
                                            </CheckBox.Style>
                                            <StackPanel Orientation="Horizontal" Margin="10 0 0 0">
                                                <TextBlock Text="{Binding Track.Title}" FontSize="14" FontWeight="SemiBold"/>
                                                <TextBlock Text=" - " Foreground="LightGray"/>
                                                <TextBlock Text="{Binding Track.Artist}" Foreground="LightGray" FontSize="12" VerticalAlignment="Bottom"/>
                                            </StackPanel>
                                        </CheckBox>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <Grid Grid.Row="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Content="Mégsem" Command="{Binding CancelCreatePlaylistCommand}" Grid.Column="0"
                                Background="Transparent" Foreground="White" BorderBrush="#88FFFFFF" BorderThickness="1" 
                                Padding="12" Cursor="Hand" FontWeight="Bold">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="12"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <Button Content="Mentés" Command="{Binding SavePlaylistCommand}" Grid.Column="2"
                                Background="#fc3c44" Foreground="White" BorderThickness="0" 
                                Padding="12" Cursor="Hand" FontWeight="Bold">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="12"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>